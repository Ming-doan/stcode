---
name: db-migrations
description: Write and review database migrations. Use when a schema change is requested.
---

# Migrations

Every migration is reversible. Write the `down` first — if you cannot, the change needs
a different shape.
