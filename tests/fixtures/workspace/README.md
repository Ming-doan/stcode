# ledger

A deliberately small project, used as the agent's working directory in
`tests/integration/`. Nothing here is imported by the test suite — it exists to be
read, grepped and edited by real tools, so the integration tests exercise the same
paths a session does.
